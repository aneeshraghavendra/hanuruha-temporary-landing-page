Place your static assets here.

Expected filenames (used by `index.html`):
- `assets/logo.png` (navbar logo)
- `assets/banner.pdf` (Download Brochure button)

If your files have different names, either rename them to match or update the paths in `index.html`.
